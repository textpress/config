{
    "env": {
        "doc": "The application environment.",
        "format": [
            "production",
            "development",
            "test"
        ],
        "default": "development",
        "env": "NODE_ENV"
    },
    "AWS": {
        "defaults": {
            "region": {
                "doc": "AWS region",
                "format": "String",
                "default": "us-east-1"
            }
        }
    }
}